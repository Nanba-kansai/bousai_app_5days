# 避難所検索機能の移植用プロンプト

以下の要件を、既存システムの設計・フレームワーク・コーディング規約に合わせて実装してください。
既存のホーム画面、ログイン機能、避難所登録機能、他の画面のレイアウトや動作は変更しないでください。今回の対象は避難所検索に関係する機能だけです。

## 目的

利用者が登録済みの避難所を地図で確認し、自分に必要な支援条件を選択して検索できるようにする。

## 必須機能

### 1. 避難所検索画面

避難所検索画面に、次の要素を表示する。

- 登録済みの地域を現在地として扱う地図
- 現在地の表示
- 避難所の位置を示す地図上のピン
- 次の案内文
  - 「次の項目に当てはまる場合はチェックを入れて、検索してください。」
- 次のチェックボックス
  - `🤰 妊婦`
  - `♿ 車いす利用者`
  - `🐾 ペット同伴`
  - `🤝 障がいのある方`
- 目立つ色の「検索」ボタン

条件選択エリアは、枠線・背景色・十分な余白などで、地図と区別して見やすくする。

### 2. 地図

- 現在地はブラウザのGPSではなく、システムに登録された地域を使う。
- 初期表示の中心は、登録地域の緯度・経度にする。
- 住所が登録されている避難所は、住所を緯度・経度へ変換して保存する。
- 緯度・経度が保存されている避難所は、地図上にマーカーを表示する。
- マーカーをクリックすると、避難所名と住所を表示する。
- 現在地の説明用ポップアップは、地図を隠さないよう初期状態では閉じておく。必要であればマーカークリック時に表示する。
- 地図タイルは、利用規約に従った安定した配信元を使用する。OpenStreetMapを使用する場合は、標準タイルサーバーへの過剰なアクセスを避け、適切な帰属表示を行う。
- 地図タイルを取得できない場合でも、地図コンテナや避難所情報が壊れないようにする。

### 3. 避難所登録との連携

避難所登録時に、既存の施設名・住所・詳細情報に加えて、次の対応条件を保存する。

```json
{
  "pregnant": true,
  "wheelchair": false,
  "pet": true,
  "disability": false
}
```

住所が入力された場合は、住所検索APIなどで緯度・経度を取得し、取得できたときだけ次の項目を保存する。

```json
{
  "latitude": 40.8246,
  "longitude": 140.7400
}
```

住所変換に失敗しても、避難所登録自体は成功させる。失敗時は緯度・経度なしとして扱い、地図にはマーカーを表示しない。

### 4. 条件検索

検索ボタンを押したときだけ、チェックされた条件を検索に使用する。

- 何もチェックしなければ、登録済みの全避難所を表示する。
- 複数選択時は、選択されたすべての条件に対応している避難所だけを表示する。
- 例えば「車いす利用者」と「ペット同伴」を選択した場合、両方が `true` の施設だけを表示する。
- 検索結果には、避難所名、住所、詳細情報、対応している条件のアイコンを表示する。
- 検索結果の地図には、緯度・経度を持つ検索結果だけをマーカー表示する。
- 該当施設がない場合は、「該当する避難所が見つかりませんでした」と表示する。

### 5. チェック状態のリセット

チェックボックスの状態は、検索処理のために一時的に使用するだけにする。

- 検索ボタンを押したときは、その時点のチェック内容を検索処理へ渡す。
- 検索結果を表示したら、検索条件の一時保存を破棄する。
- 別画面へ移動して避難所検索画面へ戻った場合も、すべて未チェック状態にする。
- 避難所検索画面を開くたびに、チェックボックスは未チェックから開始する。
- 前回のチェック状態を、次回の検索画面に自動復元しない。

## データ互換性

既存の避難所データに `address`、`details`、条件フラグ、緯度・経度が存在しない場合でも、エラーにしない。

- 住所がない場合は「未登録」と表示する。
- 詳細情報がない場合は「未登録」と表示する。
- 条件フラグがない場合は `false` として扱う。
- 緯度・経度がない施設は一覧には表示するが、地図上のピンは表示しない。
- 既存データを勝手に削除・上書きしない。

## UI・アクセシビリティ

- チェックボックスとラベルをクリックしやすくする。
- アイコンだけに意味を持たせず、必ず日本語ラベルも表示する。
- `aria-label`、見出し、フォームラベルを適切に設定する。
- 検索ボタンは通常の文字色と背景色の差を十分に確保する。
- PCとスマートフォンの両方で、地図と条件選択が重ならないようにする。
- 地図、条件選択、検索ボタンの順序を利用者が理解しやすい構成にする。

## 実装手順

1. 既存の避難所登録データ構造と検索処理を確認する。
2. 既存の設計に合わせて、条件フラグ・住所・詳細・緯度・経度を保存できるようにする。
3. 住所から緯度・経度を取得する処理を追加する。
4. 避難所検索画面に地図と現在地を追加する。
5. チェックボックスと検索ボタンを追加する。
6. 検索条件を検索処理へ渡し、全条件一致で絞り込む。
7. 検索結果の地図へ施設ピンを表示する。
8. 画面遷移時にチェック状態がリセットされることを確認する。
9. 既存画面に不要な変更がないことを確認する。

## 受け入れ条件

次のテストをすべて満たすこと。

- 避難所検索画面が正常に表示される。
- 登録地域を中心とした地図が表示される。
- 登録済み住所に対応する避難所ピンが表示される。
- 条件アイコンと日本語ラベルが表示される。
- 条件を選択して検索すると、条件に合う施設だけが表示される。
- 条件未選択で検索すると、全施設が表示される。
- 検索結果に住所と詳細情報が表示される。
- 検索画面へ戻ると、全チェックボックスが未チェックになる。
- 別画面を経由して検索画面へ戻っても、全チェックボックスが未チェックになる。
- 住所変換に失敗しても登録処理が失敗しない。
- 既存の住所なしデータでも画面がエラーにならない。
- 既存のホーム画面や他の機能に回帰がない。

実装後は、変更したファイル、動作確認したテスト、既存システム側で追加設定が必要な外部APIや地図タイルの情報を簡潔に報告してください。
